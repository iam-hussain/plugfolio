/**
 * Persistent chrome (Dev Spec §03) — the top bar, site footer and bottom tab
 * bar that wrap every screen. Product-agnostic, so it lives in components/.
 */
export { AppTopBar } from "./app-top-bar";
export { ShopperTabBar } from "./shopper-tab-bar";
export { ShopperShell } from "./shopper-shell";
export { SiteFooter, type SiteFooterProps } from "./site-footer";
