/**
 * Brand chrome — the Plugfolio mark and lockups (Brand Guidelines v1.1).
 * Product-agnostic, so it lives in components/ (not a feature). Import from here.
 */
export { PlugMark, type PlugMarkProps } from "./plug-mark";
export { Wordmark, type WordmarkProps } from "./wordmark";
export { Logo, type LogoProps } from "./logo";
