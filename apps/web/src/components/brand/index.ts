/**
 * Brand chrome — the Plugfolio mark and lockups (Brand Guidelines v1.1).
 * The components now live in @plugfolio/ui so every app shares one mark;
 * this module stays as the web app's import path.
 */
export {
  PlugMark,
  type PlugMarkProps,
  Wordmark,
  type WordmarkProps,
  Logo,
  type LogoProps,
} from "@plugfolio/ui";
