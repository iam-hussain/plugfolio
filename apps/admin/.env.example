# Admin app (ADR-0014). Copy to .env.local.
DATABASE_URL=postgresql://plugfolio:plugfolio@localhost:5432/plugfolio
# 32+ chars; independent from the web app's secret.
AUTH_SECRET=change-me-to-a-32-char-minimum-secret
